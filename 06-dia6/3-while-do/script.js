//el codigo do while se ejecuta al menos una vez
// ademas ejecuta el codigo y despues verifica la condicion

let vidas = 5;

do{
  console.log("Vidas: "+ vidas);
  }
  while(vidas >10);
//la consola mostrara igualmente le codigo aunque no se cumpla la condicion, ya que el codigo se ejcuta al menos 1 vez

//prompt es como alert pero con un imput
//  primero promt (va el texto arriba del impot, va un mensaje que va dentro del imput, si le asignamos una variable quedara el valor que entrego el usuario como la variable)
let jac =prompt("Escribe algo","hola")