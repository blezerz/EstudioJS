//en loop while es similar al for ya que primero verifica la condicion, la diferencia es que en el while no se necesita un contador, ni la actualizacion  solo necesitoa el condicional ej while (condicion){Codigo}

let nombre;

while (nombre !="Bim"){
  nombre= prompt("Dime mi nombre")
  breck;
}
//write permte escribir codigo html
document.write("<h1>Hola " + nombre + "</h1>")